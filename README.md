## Answers to Questions

### 1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?

Ans-1: The difference between this in Java script, this is select HTML elements fo use DOM. getElementsByClassName, and querySelector, querySelectorAll work in many ways.

### 2. How do you create and insert a new element into the DOM?

Ans-2: Create a insert new elements the DOM, added methods like document.createElement(), appendChild(), append() etc.


### 3. What is Event Bubbling? And how does it work?

Ans-3: An event start from the target element, than back "Bubble up" to came parent elements at DOM tree.

### 4. What is Event Delegation in JavaScript? Why is it useful?

Ans-4: Event Delegation is a technique in JavaScript where adding event listeners to multiple child elements, adding a event listener to parent element and use to handle the events.

### 5. What is the difference between preventDefault() and stopPropagation() methods?

Ans-5: Difference Between preventDefault() and stopPropagation() Both are used JavaScript event handling. There is the Difference:
** preventDefault() use to handle default browser behavior of an element.
*** stopPropagation() is stops the event from Bubbling up in the DOM tree.

